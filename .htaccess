<IfModule mod_rewrite.c>
    RewriteEngine On

    # Work both at the domain root and in a subfolder such as /luckydraw.
    # Stop rewriting after Apache has internally entered Laravel's /public folder.
    RewriteRule ^public(?:/|$) - [L]
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
