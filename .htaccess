<IfModule mod_rewrite.c>
    RewriteEngine On

    # Hostinger Web/Cloud hosting serves public_html as the document root.
    # Keep Laravel in the repository root and route public requests into /public.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
